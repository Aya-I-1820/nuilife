<!DOCTYPE html>
<html>
  <head>
    <title>ぬいらいふ</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag('favicon.ico') %>
  </head>

  <body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
    <div class="container-fluid">
      <div class="row">
       
  <div class="container-fluid">
    <div class="row">

      <% if user_signed_in? %>
        <!-- ヘッダー（モバイルのみ表示） -->
        <div class="d-md-none d-flex align-items-center justify-content-between border-bottom py-2">
          <button class="btn btn-outline-primary ms-2" type="button"
                  data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar"
                  aria-controls="mobileSidebar" aria-label="メニュー">
            ☰ メニュー
          </button>
          <%= link_to root_path, class: "me-3 text-decoration-none" do %>
            ぬいらいふ
          <% end %>
        </div>

        <!-- モバイル：オフキャンバス -->
        <div class="offcanvas offcanvas-start" tabindex="-1" id="mobileSidebar" aria-labelledby="mobileSidebarLabel">
          <div class="offcanvas-header">
            <h5 class="offcanvas-title" id="mobileSidebarLabel">メニュー</h5>
            <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="閉じる"></button>
          </div>
          <div class="offcanvas-body">
            <%= render "layouts/sidebar" %>
          </div>
        </div>

        <!-- 左側メニュー -->
         <aside class="d-none d-md-block col-md-2 col-lg-2 bg-light border-end p-3 position-sticky top-0 sidebar-full">
          <%= render "layouts/sidebar" %>
        </aside>

        <!-- メインコンテンツ -->
        <main class="col-12 col-md-10 p-3 p-sm-">
          <%= render "devise/shared/flash" %>
          <%= yield %>
        </main>

      <% else %>
        <!-- 未ログイン時：コンテンツを全幅表示（メニュー無し） -->
        <main class="col-12 p-4">
          <%= render "devise/shared/flash" %>
          <%= yield %>
        </main>
      <% end %>

      </div>
    </div>
  </body>
</html>
